__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store

# selftest build artifacts
.coverage
coverage.json
.venv/
*.egg-info/

actionlint

# OMC (oh-my-claudecode) per-session runtime state — operational artifacts,
# never source. One slipped into a commit via a broad `git add` (2026-08-21).
.omc/
